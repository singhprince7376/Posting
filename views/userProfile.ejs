<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= user.name %>'s Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #232526 0%, #414345 100%);
            color: #f1f1f1;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 1100px;
            margin: 40px auto;
            display: flex;
            flex-wrap: wrap;
            gap: 32px;
            background: rgba(30, 32, 34, 0.95);
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.25);
            padding: 32px;
        }
        .left-side, .right-side {
            background: rgba(44, 47, 51, 0.98);
            padding: 28px 22px;
            border-radius: 14px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.18);
            flex: 1 1 350px;
            min-width: 320px;
        }
        .profile-header {
            display: flex;
            align-items: center;
            gap: 18px;
            margin-bottom: 24px;
            animation: fadeIn 1s;
        }
        .profile-header img {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            border: 3px solid #4db8ff;
            object-fit: cover;
            box-shadow: 0 2px 8px #222;
            transition: transform 0.3s;
        }
        .profile-header img:hover {
            transform: scale(1.08) rotate(-2deg);
        }
        .profile-header h2 {
            font-size: 2rem;
            font-weight: 700;
            color: #4db8ff;
            margin: 0;
        }
        .btn {
            background: linear-gradient(90deg, #4db8ff 0%, #007acc 100%);
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            color: #fff;
            cursor: pointer;
            font-weight: 600;
            font-size: 1rem;
            margin-top: 16px;
            margin-right: 8px;
            box-shadow: 0 2px 8px #222;
            transition: background 0.3s, transform 0.2s, box-shadow 0.2s;
            outline: none;
            text-decoration: none;
            display: inline-block;
        }
        .btn:hover {
            background: linear-gradient(90deg, #007acc 0%, #4db8ff 100%);
            transform: translateY(-2px) scale(1.04);
            box-shadow: 0 4px 16px #007acc55;
            color: #fff;
        }
        .post {
            background: rgba(34, 36, 38, 0.98);
            padding: 22px 18px;
            border-radius: 12px;
            margin-top: 22px;
            margin-bottom: 8px;
            box-shadow: 0 2px 8px #222;
            animation: fadeInUp 0.8s;
            transition: box-shadow 0.3s, transform 0.3s;
        }
        .post:hover {
            box-shadow: 0 6px 24px #4db8ff33;
            transform: translateY(-2px) scale(1.01);
        }
        .post h3 {
            color: #4db8ff;
            margin-bottom: 8px;
            font-size: 1.2rem;
        }
        .post h4 {
            color: #ffb84d;
            margin-bottom: 8px;
            font-size: 1.1rem;
        }
        .post small {
            display: block;
            color: #aaa;
            margin-bottom: 12px;
        }
        @media (max-width: 900px) {
            .container {
                flex-direction: column;
                padding: 18px;
            }
            .left-side, .right-side {
                min-width: unset;
                padding: 18px 10px;
            }
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px);}
            to { opacity: 1; transform: translateY(0);}
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px);}
            to { opacity: 1; transform: translateY(0);}
        }
        a {
            color: #4db8ff;
            text-decoration: none;
            font-weight: 600;
        }
        a:hover {
            text-decoration: underline;
            color: #ffb84d;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container">
        <div class="left-side">
            <div class="profile-header">
                <img src="<%= user.profilePic %>" alt="Profile Picture" />
                <h2><%= user.name %></h2>
                <p style="color:#aaa; margin-top: 8px;">
                    Followers: <a href="/user/<%= user._id %>/followers" style="color:#4db8ff; text-decoration: underline;"><%= user.followers ? user.followers.length : 0 %></a>
                </p>
            </div>
            <% if (currentUser && currentUser._id.toString() !== user._id.toString()) { %>
                <form action="/user/<%= user._id %>/follow" method="POST" style="display:inline;">
                    <button class="btn" type="submit"><%= isFollowing ? 'Unfollow' : 'Follow' %></button>
                </form>
            <% } %>
            <a href="/all-posts" class="btn">Back to All Posts</a>
        </div>
        <div class="right-side">
            <h3 style="color:#4db8ff;"><%= user.name %>'s Posts</h3>
            <% if (posts.length === 0) { %>
                <p style="color:#aaa;">No posts yet.</p>
            <% } else { %>
                <% posts.forEach((post) => { %>
                    <div class="post">
                        <h3><%= user.name %></h3>
                        <small>Posted on: <%= post.createdAt.toDateString() %></small>
                        <h4><%= post.title %></h4>
                        <p><%= post.content %></p>
                        <% if (post.image) { %>
                            <img src="<%= post.image %>" alt="Post Image" style="max-width: 500px; height: auto; margin-top: 10px; border-radius: 8px; display: block;">
                        <% } %>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>
</body>
</html>
